[package]
name = "bench"
description = "Criterion benches (corpus sweep, per-assertion p99 enforcement) — out of default-members"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
syntax = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "parse"
harness = false
