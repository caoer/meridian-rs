---
paths:
  - tasks/**
arm: off
armed_rev: 1aa7b939cee536b2
cites: decisions/why-this-convention
author: author-x
---

# candidate (an arming-proposal fixture)

A candidate convention page the meta-convention judges when it proposes to arm.
Its `arm` / `armed_rev` / `cites` / `author` frontmatter carry the arming
attestation the meta-convention's three preconditions read.

```starlark
def check_change(change):
    pass
```
