[package]
name = "render"
description = "The compiled-in render plane: Renderer trait + tree-walker producing the token-efficient text projection (readText parity), with the block-elision and link-decoration hook points"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
lock = { workspace = true }
model = { workspace = true }
wire = { workspace = true }
wire-map = { workspace = true }

[dev-dependencies]
syntax = { workspace = true }
