[package]
name = "render"
description = "The compiled-in render plane: Renderer trait + tree-walker producing the token-efficient TOON-compact projection (U15), with its own TOON encoder and the block-elision and link-decoration hook points"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
lock = { workspace = true }
model = { workspace = true }
# the wikilink grammar's owner: the claim-link decoration reads its block-ref
# slot from `syntax::block_slot` rather than re-spelling the grammar here.
syntax = { workspace = true }
wire = { workspace = true }
wire-map = { workspace = true }

[dev-dependencies]
# fixture addresses for the lock elision gate
addr = { workspace = true }
