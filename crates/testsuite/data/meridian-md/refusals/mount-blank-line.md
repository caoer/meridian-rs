---
type: meridian-config
version: 1
---

# My system (refused: malformed-line)

A blank line inside the block body is not `key: value`. Prose about a mount
belongs BESIDE the block, not inside it — that is what the scoping law buys, and
it keeps the block grammar at one spelling per fact.

```meridian-mount
name: field-notes
path: /Users/Shared/projects/field-notes

kind: vault
vault: field-notes
```
