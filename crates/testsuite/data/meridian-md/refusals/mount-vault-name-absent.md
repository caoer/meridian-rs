---
type: meridian-config
version: 1
---

# My system (refused: missing-required-field)

The kind-conditional half of the same class: `vault:` is required when
`kind: vault`, because the mount table is a THREE-way map — canonical name,
Obsidian vault name, local path. Without the vault name the map has two legs.
The refusal names the kind that requires the field.

```meridian-mount
name: field-notes
path: /Users/Shared/projects/field-notes
kind: vault
```
