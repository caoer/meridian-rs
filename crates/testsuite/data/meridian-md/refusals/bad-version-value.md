---
type: meridian-config
version: one
---

# My system (refused: bad-value)

`version:` must be an integer. A non-integer is a type violation, not an
unsupported version — the two are different reason words because the operator's
fix differs: one is a typo, the other is an upgrade.
