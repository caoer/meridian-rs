# My system (refused: no-frontmatter)

This file has no frontmatter at all, so it cannot say what it is or which schema
it speaks. Refused at line 1.

`MERIDIAN_CONFIG` can aim the engine at any path, so a config that cannot
identify itself must not be half-loaded as one.

```meridian-mount
name: field-notes
path: /Users/Shared/projects/field-notes
vault: field-notes
```
