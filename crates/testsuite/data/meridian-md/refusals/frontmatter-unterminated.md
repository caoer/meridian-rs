---
type: meridian-config
version: 1

# My system (refused: no-frontmatter)

The frontmatter fence opens at line 1 and never closes. Refused at line 1,
naming that the fence never closed — the same reason word as a file with no
frontmatter, because the outcome is identical: the engine has no frontmatter to
read.
