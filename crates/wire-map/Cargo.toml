[package]
name = "wire-map"
description = "The named model→wire projection seam: tree-flatten + text_prefix_16b + node ordering as a tested library function — never bin code"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
model = { workspace = true }
wire = { workspace = true }

[dev-dependencies]
syntax = { workspace = true }
